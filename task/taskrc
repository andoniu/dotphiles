#
# dotphiles : https://github.com/dotphiles/dotphiles
#
# Setup taskwarrior
#
# Authors:
#   Ben O'Hara <bohara@gmail.com>
#

# Here is an example of entries that use the default, override and blank values
#   variable=foo   -- By specifying a value, this overrides the default
#   variable=      -- By specifying no value, this means no default
#   #variable=foo  -- By commenting out the line, or deleting it, this uses the default

# Use the command 'task show' to see all defaults and overrides

# Files
data.location=~/.task
curses=on
# Color theme
include ~/.task/colors/dark-16.theme
#include ~/task.theme
default.project = profiler
uda.estimate.label=Estimate
uda.estimate.type=duration

taskd.certificate=~/.task/private.certificate.pem
taskd.key=~/.task/private.key.pem
taskd.ca=~/.task/ca.cert.pem
taskd.server=taskwarrior.inthe.am:53589
taskd.credentials=inthe_am/ovidiu.andoniu/d952c75c-2520-474d-be4c-b1261266be91
taskd.trust=ignore hostname
